name: Python Demo

on:
  push:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
     - uses: actions/checkout@v4
     - name: List files
       run: ls -la
     - uses: actions/setup-python@v5
       with:
         python-version: "3.12"
     - name: Run Python App
       run: python app.py
